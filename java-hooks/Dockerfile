FROM openjdk:11-jre-slim
WORKDIR /app
# Scaffolding mock for enterprise Java hooks (Spring Boot)
# In a real environment, you would copy the built target/*.jar here
EXPOSE 8080
CMD ["echo", "Enterprise Hooks Java 11 service is mock-running on port 8080"]
